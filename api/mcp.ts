/**
 * Vercel entrypoint when the project's Root Directory is the REPOSITORY ROOT.
 * The handler itself lives in apps/mcp/src/handler.ts; see the note there.
 */
import handler from '../apps/mcp/src/handler.js';

export default handler;
