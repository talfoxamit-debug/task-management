/**
 * Vercel entrypoint when the project's Root Directory is apps/mcp.
 * The handler itself lives in ../src/handler.ts; see the note there.
 */
import handler from '../src/handler.js';

export default handler;
